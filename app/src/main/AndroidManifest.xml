<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.gymnasium_lappersdorf.gymlapapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Stundenplan.AddHourActivity"
            android:parentActivityName=".MainActivity" />

        <!-- google maps (KEY ONLY FOR TESTING) TODO: Add secure api key for release -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBfUZsc_CHQ7FM18_L9bcLnXuHt9s0LcF0" />

        <!-- google maps end -->


        <!--region services-->

        <service
            android:name=".HausaufgabenPlaner.JobService"
            android:permission="android.permission.BIND_JOB_SERVICE">

        </service>

        <!--endregion-->

    </application>

</manifest>